# 精简模块 & 添加一言功能

## 一、评估可移除模块

| 模块 | 行数 | 评估 | 建议 |
|------|------|------|------|
| MarketCloudView | 116 | 仅嵌入外部 iframe (dapanyuntu.com)，无自有逻辑 | **移除** |
| UserInfoView | 220 | 用户信息页，功能已简化，可合并到设置页 | **移除** |
| research store | 139 | 研究报告存储，无独立页面使用 | **移除** |
| notifications store | 137 | Bark webhook，低频功能 | **移除** |
| MysticPickView | 756 | 玄学选股，娱乐功能 | **保留** |
| AskView (AI 问答) | 732 | 核心 AI 功能 | **保留** |
| PortfolioView | 582 | 核心持仓功能 | **保留** |
| AlertsView | 589 | 核心预警功能 | **保留** |

## 二、精简后的路由 (11 个)

```
/           → HomeView (概览 + 一言)
/sectors    → SectorsView
/sector/:id → SectorDetailView
/stock/:symbol → StockDetailView
/search     → StockSearchView
/favorites  → MyFavoritesView
/portfolio  → PortfolioView
/alerts     → AlertsView
/ask        → AskView (AI 问答)
/mystic     → MysticPickView (玄学选股)
/settings   → SettingsView
/login      → LoginView
```

## 三、添加一言功能

### 3.1 创建一言 API 模块
**新建文件:** `src/api/hitokoto.ts`

```typescript
// 一言 API: https://v1.hitokoto.cn/
// 支持分类: a(动画), b(漫画), c(游戏), d(文学), e(原创), f(网络), g(其他)
// 投资相关分类: d(文学), e(原创), f(网络)

const HITOKOTO_API = 'https://v1.hitokoto.cn/'

export interface HitokotoSentence {
  hitokoto: string
  from: string
  from_who: string | null
  type: string
}

// 默认激励语（API 失败时使用）
const DEFAULT_SENTENCES: HitokotoSentence[] = [
  { hitokoto: '投资最重要的品质是耐心和纪律。', from: '巴菲特', from_who: null, type: 'e' },
  { hitokoto: '别人恐惧时我贪婪，别人贪婪时我恐惧。', from: '巴菲特', from_who: null, type: 'e' },
  { hitokoto: '不要把所有鸡蛋放在一个篮子里。', from: '投资格言', from_who: null, type: 'e' },
  { hitokoto: '市场总是对的，错的是你的判断。', from: '投资格言', from_who: null, type: 'e' },
  { hitokoto: '成功的投资不在于你做了什么，而在于你没有做什么。', from: '投资格言', from_who: null, type: 'e' },
  { hitokoto: '时间是优秀企业的朋友，是平庸企业的敌人。', from: '巴菲特', from_who: null, type: 'e' },
  { hitokoto: '风险来自于你不知道自己在做什么。', from: '巴菲特', from_who: null, type: 'e' },
  { hitokoto: '股市是转移耐心者财富到有耐心者的工具。', from: '投资格言', from_who: null, type: 'e' },
  { hitokoto: '买入好公司的股票，然后永远不要卖出。', from: '费雪', from_who: null, type: 'e' },
  { hitokoto: '投资的秘诀在于，当别人贪婪时恐惧，当别人恐惧时贪婪。', from: '格雷厄姆', from_who: null, type: 'e' },
]

let lastIndex = -1

export async function fetchHitokoto(categories = 'def'): Promise<HitokotoSentence> {
  try {
    const url = `${HITOKOTO_API}?c=${categories}&encode=json`
    const r = await fetch(url, { signal: AbortSignal.timeout(3000) })
    if (!r.ok) throw new Error(`HTTP ${r.status}`)
    const data = await r.json() as HitokotoSentence
    return data
  } catch {
    // API 失败时返回随机默认语
    return getRandomDefault()
  }
}

function getRandomDefault(): HitokotoSentence {
  let idx: number
  do {
    idx = Math.floor(Math.random() * DEFAULT_SENTENCES.length)
  } while (idx === lastIndex && DEFAULT_SENTENCES.length > 1)
  lastIndex = idx
  return DEFAULT_SENTENCES[idx]
}
```

### 3.2 在概览页展示一言
**修改文件:** `src/views/HomeView.vue`

在日期栏下方、指数上方添加一言展示：

```vue
<!-- 一言 -->
<section class="hitokoto-bar" @click="refreshHitokoto">
  <span class="hk-text">「{{ hitokoto.hitokoto }}」</span>
  <span v-if="hitokoto.from_who || hitokoto.from" class="hk-from small muted">
    —— {{ hitokoto.from_who || hitokoto.from }}
  </span>
</section>
```

样式：
```css
.hitokoto-bar {
  padding: var(--space-3) var(--space-4);
  margin-bottom: var(--space-4);
  border-left: 3px solid var(--color-link);
  background: var(--color-bg-elevated);
  border-radius: 0 var(--radius-md) var(--radius-md) 0;
  cursor: pointer;
  transition: background var(--transition-fast);
}
.hitokoto-bar:hover { background: var(--color-bg-muted); }
.hk-text { font-style: italic; color: var(--color-ink); }
.hk-from { margin-left: var(--space-2); }
```

Script:
```typescript
import { ref, onMounted } from 'vue'
import { fetchHitokoto, type HitokotoSentence } from '../api/hitokoto'

const hitokoto = ref<HitokotoSentence>({ hitokoto: '投资最重要的品质是耐心和纪律。', from: '', from_who: null, type: 'e' })

async function refreshHitokoto() {
  hitokoto.value = await fetchHitokoto()
}

onMounted(refreshHitokoto)
```

### 3.3 刷新时机
- 首次进入概览页时加载（onMounted）
- 点击一言区域刷新
- 切换板块时刷新（watch topicStore.activeId）

## 四、要删除的文件

1. `src/views/MarketCloudView.vue`
2. `src/views/UserInfoView.vue`
3. `src/stores/research.ts`
4. `src/stores/notifications.ts`
5. `src/utils/personalConfig.ts`（已简化，确认无引用后删除）

## 五、要修改的文件

1. `src/router/index.ts` — 移除 /market-cloud, /user 路由
2. `src/components/AppSidebar.vue` — 移除大盘云图、用户信息导航项
3. `src/views/HomeView.vue` — 添加一言展示
4. `src/views/SettingsView.vue` — 移除通知配置、研究相关引用
5. `src/views/StockDetailView.vue` — 检查是否引用 research store

## 六、验证

1. `npm run build` 构建成功
2. 本地 `npm run dev` 测试：
   - 概览页显示一言，点击可刷新
   - 侧边栏无已移除页面的链接
   - 所有保留页面正常工作
3. 部署后测试页面切换速度
